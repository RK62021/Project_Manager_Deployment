apiVersion: v1
kind: Service
metadata:
  name: teamflow-frontend-service
  labels:
    app: teamflow
    component: frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
  selector:
    app: teamflow
    component: frontend
